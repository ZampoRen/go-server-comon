# 基础配置
# 应用名称，用于服务注册时使用
# APP_NAME=
# 默认加载的配置后缀，如果为 prod 则加载 .env.prod  为 test 则加载 .env.test
APP_ENV=
# 默认监听的端口
LISTEN_ADDR=:8888
# body 的最大值 (默认 200*1024*1024)
MAX_REQUEST_BODY_SIZE=
# 日志级别，默认 info ，可选（ trace,debug,info,notice,warn,error,fatal）
LOG_LEVEL=


# Mysql 相关配置
MYSQL_DSN="user:password@tcp(localhost:3306)/dbname?charset=utf8mb4"
# 最大打开连接数（默认 100）
MYSQL_MAX_OPEN_CONNS=200
# 最大空闲连接数（默认 10）
MYSQL_MAX_IDLE_CONNS=20
# 连接最大生存时间（默认 1 小时）
MYSQL_CONN_MAX_LIFETIME=2h
# 连接最大空闲时间（默认 10 分钟）
MYSQL_CONN_MAX_IDLE_TIME=15m

# Redis 相关配置
REDIS_ADDR="localhost:6379"
REDIS_PASSWORD="password"
# Redis 数据库编号（默认 0）
REDIS_DB=1
# 最大连接数 (默认 100）
REDIS_POOL_SIZE=200
# 最小空闲连接数（默认 10）
REDIS_MIN_IDLE_CONNS=20
# 最大空闲连接数（默认 30）
REDIS_MAX_IDLE_CONNS=50
# 空闲连接超时时间（默认 5m，格式如 "5m", "10m"）
REDIS_CONN_MAX_IDLE_TIME="10m"
# 连接建立超时（默认 5s，格式如 "5s", "10s"）
REDIS_DIAL_TIMEOUT="10s"
# 读操作超时（默认 3s，格式如 "3s", "5s"）
REDIS_READ_TIMEOUT="5s"
# 写操作超时（默认 3s，格式如 "3s", "5s"
REDIS_WRITE_TIMEOUT="5s"
